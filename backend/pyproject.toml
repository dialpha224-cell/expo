[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "afrocrown-backend"
version = "1.0.0"
description = "AfroCrown Backend API"
requires-python = ">=3.9"

[tool.ruff]
line-length = 120
target-version = "py39"
